server:
  port: 8081

spring:
  cloud:
    server:
      git:
        # url de repositorio donde estan ubidados los yml de los proyectos
        uri: https://github.com/marianoldm/BootcampSpringCloud.git
        # especificamos donde esta los archivos de configuracion de nuestros proyectos
        searchPaths: config-data
  security:
    user:
      name: root
      password: s3cr3t